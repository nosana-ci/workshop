# .nosana-ci.yml
nosana:
  description: Build, Test and Deploy dApps!

global:
  image: node
  trigger:
    push:
      branches:
        - main

jobs:
  - name: build
    commands:
      - echo "building..."
      - npm ci
      - npx hardhat compile
    artifacts:
      - name: node_modules
        path: .

  - name: test
    environment:
      REPORT_GAS: "true"
    commands:
      - echo "testing..."
      - npx hardhat test
    resources:
      - name: node_modules

  - name: deploy
    commands:
      - echo "deploying..."
    resources:
      - name: my-build
